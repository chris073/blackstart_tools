# Tools API (FastAPI + uvicorn). Same pins as repo-root requirements.txt API block.
# MQTT explorer (/mqtt/ws) needs paho-mqtt.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
asyncssh>=2.14.0,<3
pymodbus>=3.6.0,<4
paho-mqtt>=2.1.0,<3
