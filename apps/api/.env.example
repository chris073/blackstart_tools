# Optional — apps/api/.env.local (gitignored) or repo-root .env.local
#
# API dev default port is 8010. Override with:
# API_PORT=8000
#
# If you only set apps/web/.env.local → NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:PORT
# the API launcher reads that port automatically so both match.
#
# UVICORN_HOST=127.0.0.1
