# python-tools

This folder contains the original standalone Python tools from the repo, consolidated under one directory.

For now, run them directly with Python (each tool has its own requirements file when needed).

Examples:

```bash
python packages/python-tools/rtac/tag_editor.py
```

